<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.rxjava2.android.samples.ui.rxbus.RxBusActivity">

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center"
        android:layout_marginTop="12dp"
        android:text="Hello World!"
        android:textColor="@android:color/black" />

    <TextView
        android:id="@+id/textView_two"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center|center"
        android:layout_marginBottom="40dp"
        android:layout_marginTop="12dp"
        android:text="Hello World!"
        android:textColor="@android:color/black" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|left"
        android:layout_marginBottom="40dp"
        android:layout_marginLeft="5dp"
        android:layout_marginTop="12dp"
        android:text="i can replace the Feeding App (Scenario) while sending location to server am using ||Service and  inside thread and while app destroy have condition to clear thread, In Rx Bus much simpler the job is operator to routine call, it will destroy app destroys, no need for service and thread, boilerplate code   "
        android:textColor="@android:color/black" />


    <Button
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/click_me"
        android:textColor="@android:color/black" />

</FrameLayout>